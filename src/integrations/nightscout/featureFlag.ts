// Nightscout integration — central, isolated feature flag.
// Set to false to make the entire Nightscout module a no-op.
export const NIGHTSCOUT_ENABLED = true;

// Where the settings page lives (kept here so nothing else has to know).
export const NIGHTSCOUT_SETTINGS_PATH = "/settings/nightscout";
